const mongoose = require("mongoose");
const eyeglassSchema = new mongoose.Schema({
  name: String,
  price: Number,
  img: String,
  model: String,
  category: String,
  description: String
});

const Eyeglass = mongoose.model("Eyeglass", eyeglassSchema);
module.exports = Eyeglass;
